package io.github.afthabek.minimaldictionary.ui.theme

import androidx.compose.ui.graphics.Color

// Dark palette
val Ink = Color(0xFF101013) // near-black background
val Paper = Color(0xFFF2F2F4) // primary text
val Muted = Color(0xFF95959B) // IPA, POS headers, examples, tags
val Faint = Color(0xFF1E1E23) // field/surface fill

// Light palette (mirrors the dark one role for role)
val InkLight = Color(0xFF1A1A1E) // primary text
val PaperLight = Color(0xFFF7F7F9) // near-white background
val MutedLight = Color(0xFF6A6A71) // IPA, POS headers, examples, tags
val FaintLight = Color(0xFFE8E8EC) // field/surface fill
